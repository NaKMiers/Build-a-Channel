# 12 Section 03 Visual Plan

Video:
`Why Everyone Pretends To Be Busy`

Section:
`03 Busy Became Status`

Status:
`implemented as separate preview project`

## Section Goal

Show busyness as social status:

```text
busy = important
busy = needed
busy = demand
busy = near the work
```

The section should stay in the accepted simple board rhythm:

```text
real-life-ish image -> WIT reaction -> short handwritten label -> hard cut
```

No real LinkedIn logo.
No real app logos.
No private messages.
No dense dashboard collage.

## Narration

```text
The first reason people pretend to be busy is simple. Busy became status.

When someone says, "I'm so busy," it does not only mean they have many things to do. It can also mean, "Please understand that I am important."

This is why people say they are busy even when nobody asked.

You ask, "How are you?" They say, "Busy."

Which is technically not an emotion, but we accept it because everyone is tired.

Busy sounds responsible. Busy sounds needed. Busy sounds like your life has demand. And demand feels like value.

If nobody needs you, that feels scary. If everyone needs you, that also feels scary, but at least you can put it on LinkedIn.

So people fill their time. Sometimes with real work. Sometimes with meetings, calls, updates, planning documents, planning meetings, and meetings about why the planning document is not finished.

At some point, the work is not the work anymore. The work is proving that you are near the work.
```

## Board Plan

| Board | Visual Job | Image |
|---|---|---|
| `status-became-status` | Establish `BUSY = STATUS` with badge/calendar desk. | `status-badge-desk-generated.png` |
| `status-important` | Turn `"I'm so busy"` into `PLEASE UNDERSTAND I AM IMPORTANT`. | `status-badge-desk-generated.png` |
| `status-nobody-asked` | Show busy performance happening unprompted. | `busy-small-talk-generated.png` |
| `status-busy-emotion` | Deadpan board: `BUSY` is not an emotion. | `busy-small-talk-generated.png` |
| `status-demand-value` | Show `RESPONSIBLE`, `NEEDED`, `DEMAND`, `VALUE`. | `demand-value-board-generated.png` |
| `status-professional-network` | Land the LinkedIn-adjacent joke using a generic professional feed. | `generic-professional-network-generated.png` |
| `status-meeting-tower` | Make meetings/calls/updates/planning documents readable as a funny tower. | `meeting-tower-generated.png` |
| `status-near-work` | Pay off the section: proving you are near the work. | `near-the-work-generated.png` |

## WIT Role

WIT is not lazy here.

WIT is socially trapped:

- gets a fake status badge
- tries answering normally, then gets buried by `BUSY`
- points out that `BUSY` is not an emotion
- gets pulled into demand/value proof
- reluctantly posts status without a real platform logo
- hides near the meeting tower
- ends beside untouched actual work

## Approval Checks

- `Busy` as an emotion gets a clear deadpan board.
- The generic professional-networking joke does not use the real LinkedIn logo.
- Meeting tower is funny but readable.
- Final board lands `NEAR THE WORK`.
- Section remains static-board simple enough for review.
