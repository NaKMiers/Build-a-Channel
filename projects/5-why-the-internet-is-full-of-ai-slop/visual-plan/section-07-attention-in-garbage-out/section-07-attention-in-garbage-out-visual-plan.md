# Section 7 Visual Plan - Payoff: Attention In, Garbage Out

Video: `Why The Internet Is Full Of Garbage Now`
Section: `Section 7: Payoff: Attention In, Garbage Out`
Status: `draft visual plan for approval (the calm payoff; callback-heavy recap)`

> Timing note: NO `section-07-word-timings.json` yet. Local times are ESTIMATED from the 43.413s audio +
> word counts. `render` must GENERATE word-timings and re-pin every cue.

## Video-Level Direction (for context - keep identical to master)

- Audience: A2-C1 English learners (interesting-English advantage).
- Renderer: HyperFrames (composites pre-made ISOLATED assets + transparent WIT poses).
- Visual grammar: a distinct vivid base per scene + a bespoke GENERATED hero + giant varied WIT + handwritten labels. Generate-forward; no crutch base.
- Mascot: WIT - round bald white head, thick black outline, big glasses, flat white body. Transparent cutouts in `assets/poses/`. Big + high, varied side/scale/pose/emotion per scene.
- Tone on screen: this is the CALM payoff - quieter than the S5 flood. Dry, clear, non-preachy; no spammy CTA. End on WIT, eyes open.
- Payoff = recap, so callbacks are intentional and meaningful here: the S4 self-feeding ENGINE winds down to a clean `ATTENTION IN -> GARBAGE OUT` board; the "tells" reuse the earlier slop artifacts (the six-finger hand from S1, the too-perfect AI face from S3, a too-good headline). Reusing these is the point - the viewer recognizes them.
- Imagination rule: unlimited within copyright/law/YouTube community standards. Generate for new heroes; reuse earlier assets for callbacks; browse-real for bases. Parody only, no real logos/people.

## Section Overview

- Section goal: land the insight calmly and hand the viewer something useful. Recap the cause (works as built -> pays for attention -> slop wins), state the takeaway "Attention in. Garbage out.", reframe ("not broken - doing its job perfectly - that's the problem"), give the tells (six fingers / too-perfect photo / too-good story), and end empowering: you can't stop the flood but you can stop being fooled; the person who pays attention on purpose wins. Sign off on "keep your eyes open."
- Duration: `43.413s`. Timing ESTIMATED (no word-timings yet).
- Scene count: `7`.
- Scene-type rotation: recap-question / answer / big-payoff-text / ironic-perfect-machine / the-tells checklist / empowerment / calm sign-off.
- Mascot arc: asking -> calm answer -> stating the takeaway -> dry irony -> presenting the tells -> confident/empowering -> calm direct sign-off (eyes open).

## Scenes

### Scene 7.1 - "So - why is the internet full of garbage now?"

- **Local time:** `0:00-0:04` (est)
- **Role:** Recap question - reopen the title question, with the S4 engine winding down in the background. Calmer energy.
- **Composition / layout:** The S4 machine hall (callback), the self-feeding engine center-right slowing down (steam fading). WIT left at ~1/2 frame, asking. Big `"WHY?"` echo.
- **Elements:**
  - *Base:* `dark-machine-hall-1.jpg` (reuse S4) - graded a touch calmer.
  - *Center-right:* `slop-engine-loop.png` (reuse S4) - winding down, last puffs of steam.
  - *CSS:* `"why is the internet full of garbage now?"` handwritten.
- **Mascot:** pose `pointing_up_curious_open_mouth`; left, ~1/2 frame, high; expression "so... why?"
- **On-screen text:** `"WHY?"` (callback to S1.9) upper-center on "why"; small `"full of garbage now"` aside.
- **Emotion:** a calm gathering-up before the answer.
- **Insight / joke:** we circle back to the opening question, now armed.
- **Linkage / eye path:** WIT asking (left) -> the slowing engine (center-right).
- **Show-as-you-say:** base + engine + WIT at 0:00; `WHY?` on "why"; engine steam fades.
- **Sound:** the engine winding down (pitch drop); a soft question chime.
- **Color / contrast:** calmer machine hall; cream/yellow text pops.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `dark-machine-hall-1.jpg` | reuse | the S4 engine hall, calmer | base | reuse (S4) |
| `slop-engine-loop.png` | reuse | the engine, winding down | center-right, ~50% width | reuse (S4) |
| `pointing_up_curious_open_mouth.png` | pose | WIT asking | left, ~1/2 frame | reuse (library) |

(The `WHY?` + `full of garbage now` text is render CSS.)

### Scene 7.2 - "Not because AI is evil. Not because of a conspiracy. Because the internet is working exactly the way it was built to work. It pays for attention. Slop is very good at grabbing attention. So slop wins."

- **Local time:** `0:04-0:15` (est)
- **Role:** The answer. Cross out "AI evil" and "conspiracy"; slop wins the attention prize because the system pays for attention.
- **Composition / layout:** A spotlit stage base (reuse the S4/S6 stage). Two struck cards `AI IS EVIL` / `CONSPIRACY` upper-left; center a slop blob on a winner's podium holding an `ATTENTION` trophy, a sad `QUALITY` entrant lower. WIT right, calm explaining.
- **Elements:**
  - *Base:* `dark-spotlight-stage-1.jpg` (reuse S4.5/S6.7) - a spotlit stage (where the "winner" stands). Clean people-free award-podium photos were not license-safe (all real athletes), so the stage motif is reused here instead; the generated trophy hero carries the "podium" idea.
  - *Center:* `slop-wins-trophy.png` (generate) - a grey slop blob standing on a #1 podium holding up a golden `ATTENTION` trophy, a small sad `QUALITY` figure off the podium; isolated.
  - *CSS:* struck `AI IS EVIL` and `CONSPIRACY` cards; `pays for ATTENTION`.
- **Mascot:** pose `eyes_closed_talking_open_palm`; right, ~1/2 frame, high; expression calm, matter-of-fact.
- **On-screen text:** struck `"AI IS EVIL"` on "not because AI is evil"; struck `"CONSPIRACY"` on "not because of a conspiracy"; `"pays for ATTENTION"` on "it pays for attention"; `"slop wins"` on "so slop wins."
- **Emotion:** calm clarity - the unglamorous real answer.
- **Insight / joke:** slop wins because it's best at grabbing attention, which is what gets paid.
- **Linkage / eye path:** the two struck cards (upper-left) -> slop on the podium with the ATTENTION trophy (center) -> `slop wins`; calm WIT right.
- **Show-as-you-say:** base + WIT at 0:04; `AI IS EVIL` struck on "AI is evil"; `CONSPIRACY` struck on "conspiracy"; slop climbs the podium + `pays for ATTENTION` on "it pays for attention"; `slop wins` on "so slop wins."
- **Sound:** a tiny triumphant fanfare that deflates.
- **Color / contrast:** stage golds; the two struck cards greyed, the ATTENTION trophy + `slop wins` pop.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `dark-spotlight-stage-1.jpg` | reuse | spotlit stage ("winner takes the stage"); from S4.5/S6.7 | base | reuse (S4) |
| `slop-wins-trophy.png` | generate | a grey slop blob on a #1 podium holding up a golden "ATTENTION" trophy, a small sad "QUALITY" figure beside the podium; flat cartoon, thick black outline, transparent bg, isolated | center, ~42% width | new |
| `eyes_closed_talking_open_palm.png` | pose | WIT calm explaining | right, ~1/2 frame | reuse (library) |

(The struck `AI IS EVIL` / `CONSPIRACY` cards and `pays for ATTENTION` / `slop wins` are render CSS. The trophy/podium words on the asset are kept generic; the labels are CSS.)

### Scene 7.3 - "Attention in. Garbage out."

- **Local time:** `0:15-0:18` (est)
- **Role:** THE takeaway - the cleanest line of the video, as a payoff board. The engine reduced to a simple machine diagram.
- **Composition / layout:** A clean board. The S4 engine (reuse), stripped to a simple box, with `ATTENTION` flowing IN on the left and `GARBAGE` dropping OUT on the right. WIT center-left, delivering the line. Big kinetic text.
- **Elements:**
  - *Base:* `dark-machine-hall-1.jpg` (reuse, dimmed to a near-clean board) OR a clean dark base.
  - *Center:* `slop-engine-loop.png` (reuse) simplified to the in->out box; an `ATTENTION` arrow in, a `GARBAGE` chute out.
  - *CSS:* big kinetic `ATTENTION IN.` (left) -> `GARBAGE OUT.` (right).
- **Mascot:** pose `lecturing_finger_raised_eyes_closed`; center-left, ~1/2 frame, high; expression delivering the key takeaway.
- **On-screen text:** `"ATTENTION IN."` lands on "attention in"; `"GARBAGE OUT."` lands on "garbage out."
- **Emotion:** the satisfying click - the whole video in four words.
- **Insight / joke:** the machine's input is attention, its output is garbage.
- **Linkage / eye path:** `ATTENTION IN` (left) -> machine -> `GARBAGE OUT` (right); WIT delivers from center-left.
- **Show-as-you-say:** hold the board; `ATTENTION IN.` on "attention in"; beat; `GARBAGE OUT.` on "garbage out."
- **Sound:** a clean two-note "in... out" sting.
- **Color / contrast:** clean dark board; the two big kinetic phrases dominate.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `dark-machine-hall-1.jpg` | reuse | dimmed to a near-clean board | base | reuse (S4/7.1) |
| `slop-engine-loop.png` | reuse | simplified in->out machine | center, ~45% width | reuse (S4) |
| `lecturing_finger_raised_eyes_closed.png` | pose | WIT delivering the takeaway | center-left, ~1/2 frame | reuse (library) |

(The big `ATTENTION IN.` / `GARBAGE OUT.` kinetic text is render CSS.)

### Scene 7.4 - "The internet is not broken. [beat] It is doing its job perfectly. That is the problem."

- **Local time:** `0:18-0:24` (est)
- **Role:** The reframe - the uncomfortable irony. The machine isn't broken; it's working perfectly, and that's the problem.
- **Composition / layout:** The engine (reuse) running smoothly with a green `WORKING PERFECTLY ✓` check; a struck `NOT BROKEN`. WIT right, deadpan. `"that is the problem."`
- **Elements:**
  - *Base:* `dark-machine-hall-1.jpg` (reuse).
  - *Center:* `slop-engine-loop.png` (reuse) humming smoothly.
  - *CSS:* a struck `BROKEN`; a green `WORKING PERFECTLY ✓`; `"that is the problem."`
- **Mascot:** pose `deadpan_unimpressed_half_lidded`; right, ~1/2 frame, high; expression dry "...perfectly."
- **On-screen text:** struck `"BROKEN"` on "not broken"; green `"WORKING PERFECTLY ✓"` on "doing its job perfectly"; `"that is the problem."` on "that is the problem."
- **Emotion:** dry irony - the worst kind of working.
- **Insight / joke:** it's not a bug; the garbage is the intended output.
- **Linkage / eye path:** struck `BROKEN` -> green `WORKING PERFECTLY` on the smooth engine -> `that is the problem`; deadpan WIT right.
- **Show-as-you-say:** engine + WIT at 0:18; `BROKEN` struck on "not broken"; beat; green `WORKING PERFECTLY ✓` on "doing its job perfectly"; `that is the problem.` on "that is the problem."
- **Sound:** a smooth contented machine hum; a flat dry note on "problem."
- **Color / contrast:** the green check is ironically reassuring; cream payoff text.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `dark-machine-hall-1.jpg` | reuse | the engine hall | base | reuse (S4/7.1) |
| `slop-engine-loop.png` | reuse | engine humming smoothly | center, ~50% width | reuse (S4) |
| `deadpan_unimpressed_half_lidded.png` | pose | WIT dry deadpan | right, ~1/2 frame | reuse (library) |

(The struck `BROKEN`, green `WORKING PERFECTLY ✓`, and `that is the problem.` are render CSS.)

### Scene 7.5 - "But here is the small, useful upgrade. Now you know the tells. The six fingers. The photo that is a little too perfect. The story that is a little too good to be true."

- **Local time:** `0:24-0:34` (est)
- **Role:** The useful gift - the tells checklist, reusing the actual slop artifacts from earlier sections (recognition callbacks).
- **Composition / layout:** A clean evidence/detective board base. A `THE TELLS` checklist center-right with three rows, each a small callback thumbnail + label: (1) the six-finger hand, (2) the too-perfect AI face, (3) a too-good headline. WIT left, presenting the list.
- **Elements:**
  - *Base:* `evidence-desk-1.jpg` (browse) - a real desk / pinboard with a magnifying glass, no faces.
  - *Row 1:* `ai-extra-fingers-hand.png` (reuse S1.8) + `"six fingers"`.
  - *Row 2:* `ai-influencer-perfect.png` (reuse S3.2) + `"a little too perfect"`.
  - *Row 3:* a CSS too-good headline card + `"too good to be true"`.
  - *CSS:* `THE TELLS` header + three checkbox rows.
- **Mascot:** pose `presenting_screen_announcing_open_mouth`; left, ~1/2 frame, high; expression "here, look at these."
- **On-screen text:** `"THE TELLS"` on "now you know the tells"; `"six fingers"` on "the six fingers"; `"a little too perfect"` on "a little too perfect"; `"too good to be true"` on "too good to be true."
- **Emotion:** helpful, equipping the viewer.
- **Insight / joke:** the takeaway tools - three quick checks.
- **Linkage / eye path:** WIT presenting (left) -> the three checklist rows ticking top-to-bottom (center-right).
- **Show-as-you-say:** base + WIT + `THE TELLS` at 0:24; row 1 (hand) on "six fingers"; row 2 (face) on "too perfect"; row 3 (headline) on "too good to be true."
- **Sound:** three light checkbox ticks.
- **Color / contrast:** clean desk; the three callback thumbnails + check ticks read clearly.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `evidence-desk-1.jpg` | browse-real-photo | real desk / pinboard with a magnifying glass, no faces, full-HD CC0 | base | new |
| `ai-extra-fingers-hand.png` | reuse | the six-finger AI hand (S1.8) | row 1 thumbnail | reuse (S1) |
| `ai-influencer-perfect.png` | reuse | the too-perfect AI face (S3.2) | row 2 thumbnail | reuse (S3) |
| `presenting_screen_announcing_open_mouth.png` | pose | WIT presenting the list | left, ~1/2 frame | reuse (library) |

(The `THE TELLS` header, the three checkbox rows, and the too-good headline card are render CSS.)

### Scene 7.6 - "You cannot stop the flood. But you can stop being fooled by it. [pause] And on an internet built on attention, the person who pays attention on purpose [emphasis] is the one who wins."

- **Local time:** `0:34-0:41` (est)
- **Role:** Empowerment. The flood keeps going, but the clear-eyed viewer wins. WIT calm, glasses/eyes emphasized.
- **Composition / layout:** A calm real base (a bright window / clear horizon), the grey flood receding to the lower edge (motif resolving). WIT center at ~1/2 frame, calm and clear-eyed (glasses shining). `"stop being fooled"` / `"pay attention on purpose -> you win"`.
- **Elements:**
  - *Base:* `bright-window-calm-1.jpg` (browse) - a calm bright window / clear horizon, no faces.
  - *Lower edge (CSS):* the grey sludge receding (the flood can't be stopped, but it's at bay).
  - *CSS:* `"can't stop the flood"`; `"but stop being FOOLED"`; `"pay attention on purpose -> you win"`; a small glint on WIT's glasses.
- **Mascot:** pose `proud_explaining_hand_on_chest_hand_on_hip`; center, ~1/2 frame, high; expression calm, confident, clear-eyed.
- **On-screen text:** `"can't stop the flood"` on "cannot stop the flood"; `"but stop being FOOLED"` on "stop being fooled by it"; `"pay attention on purpose -> you win"` on the final line (emphasis on "wins").
- **Emotion:** quiet empowerment - you have the upper hand now.
- **Insight / joke:** you can't fix the system, but you can stop being its product.
- **Linkage / eye path:** calm WIT center (glasses glint) -> the receding flood at the bottom -> the empowerment line.
- **Show-as-you-say:** base + WIT at 0:34; `can't stop the flood` on "cannot stop the flood"; `but stop being FOOLED` on "stop being fooled"; pause; `pay attention on purpose -> you win` builds, emphasis on "wins."
- **Sound:** the flood swell fades to a calm tone; a soft glasses "ting" on the glint.
- **Color / contrast:** bright calm light; the grey flood low and receding; cream/yellow payoff text.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `bright-window-calm-1.jpg` | browse-real-photo | calm bright window / clear horizon, no faces, full-HD CC0 | base | new |
| `proud_explaining_hand_on_chest_hand_on_hip.png` | pose | WIT calm, confident, clear-eyed | center, ~1/2 frame | reuse (library) |

(The receding grey sludge, `can't stop the flood`, `but stop being FOOLED`, `pay attention on purpose -> you win`, and the glasses glint are render CSS.)

### Scene 7.7 - "We'll keep explaining the weird machine. You keep your eyes open."

- **Local time:** `0:41-0:43.413` (est)
- **Role:** The sign-off - calm, direct, no spammy CTA. End on WIT, eyes open. A small machine callback + a direct "you keep your eyes open."
- **Composition / layout:** A clean end-card base. WIT center at ~1/2 frame, calm, facing the viewer, glasses/eyes wide-and-open. A tiny `slop-engine-loop` icon lower-corner (the "weird machine" we'll keep explaining). `"keep your eyes open."`
- **Elements:**
  - *Base:* a clean calm base (reuse `bright-window-calm-1.jpg` continuation, or a clean light end-card).
  - *Lower-corner:* `slop-engine-loop.png` (reuse) small, as the "weird machine" tag.
  - *CSS:* `"we'll keep explaining the weird machine."`; big `"keep your eyes open."`
- **Mascot:** pose `pointing_at_viewer_serious_accusing`; center, ~1/2 frame, high; expression calm-direct (not angry) - eyes open, addressing the viewer.
- **On-screen text:** `"we'll keep explaining the weird machine"` on "the weird machine"; big `"keep your eyes open."` on "keep your eyes open."
- **Emotion:** calm, companionable resolve - a clean close.
- **Insight / joke:** the dry sign-off; the gift is awareness, not panic.
- **Linkage / eye path:** WIT direct center -> the small machine icon (corner) -> `keep your eyes open.`
- **Show-as-you-say:** WIT at 0:41; `we'll keep explaining the weird machine` on "the weird machine" (machine icon blinks); `keep your eyes open.` on "keep your eyes open."
- **Sound:** a soft closing tone into the outro.
- **Color / contrast:** clean light end-card; WIT + the payoff line are the focus.

**Assets:**

| Filename | Type | Description (NO prompt) | Layout / position | Reuse? |
|---|---|---|---|---|
| `bright-window-calm-1.jpg` | reuse | calm end-card base (7.6 continuation) | base | reuse (7.6) |
| `slop-engine-loop.png` | reuse | tiny "weird machine" tag | lower-corner, small | reuse (S4) |
| `pointing_at_viewer_serious_accusing.png` | pose | WIT calm-direct, eyes open | center, ~1/2 frame | reuse (library) |

(The `we'll keep explaining the weird machine` and big `keep your eyes open.` are render CSS.)

## Section Asset Summary

| Filename | Type | First scene | Reused in | Notes |
|---|---|---|---|---|
| `dark-machine-hall-1.jpg` | reuse | 7.1 | 7.3, 7.4 | the S4 engine hall (recap) |
| `slop-engine-loop.png` | reuse | 7.1 | 7.3, 7.4, 7.7 | the S4 engine winds down -> in/out board -> tiny tag |
| `dark-spotlight-stage-1.jpg` | reuse | 7.2 | - | spotlit stage (reused; clean award-podium photos had real athletes) |
| `slop-wins-trophy.png` | generate | 7.2 | - | slop blob with the ATTENTION trophy |
| `evidence-desk-1.jpg` | browse-real-photo | 7.5 | - | the-tells board base |
| `ai-extra-fingers-hand.png` | reuse | 7.5 | - | tell #1 (six fingers), from S1.8 |
| `ai-influencer-perfect.png` | reuse | 7.5 | - | tell #2 (too perfect), from S3.2 |
| `bright-window-calm-1.jpg` | browse-real-photo | 7.6 | 7.7 | calm empowerment + sign-off base |
| poses (7) | pose | - | - | `pointing_up_curious_open_mouth`, `eyes_closed_talking_open_palm`, `lecturing_finger_raised_eyes_closed`, `deadpan_unimpressed_half_lidded`, `presenting_screen_announcing_open_mouth`, `proud_explaining_hand_on_chest_hand_on_hip`, `pointing_at_viewer_serious_accusing` (7 distinct) |

Generate count: `1` new (slop-wins-trophy). This is a recap payoff, so it is deliberately CALLBACK-heavy:
it reuses the S4 engine (`slop-engine-loop.png`, the hall) and the earlier tells (`ai-extra-fingers-hand.png`
S1, `ai-influencer-perfect.png` S3) - recognition is the point. Browse bases: `2` fresh (evidence-desk,
bright-window-calm) + reuse `dark-spotlight-stage-1.jpg` (S4) for the 7.2 stage (clean people-free
award-podium photos were not license-safe). Render-CSS: `WHY?`, struck `AI IS EVIL`/`CONSPIRACY`, `pays for ATTENTION`
/ `slop wins`, big `ATTENTION IN.` / `GARBAGE OUT.`, struck `BROKEN` + green `WORKING PERFECTLY ✓` +
`that is the problem.`, the `THE TELLS` checklist + too-good headline, the receding flood + empowerment
text + glasses glint, `we'll keep explaining the weird machine`, big `keep your eyes open.`.

## Approval Checks

- each scene picturable from text alone: yes
- ~one scene per beat, scene-types varied (recap-question / answer / payoff-text / ironic-machine / tells / empowerment / sign-off): yes
- every scene has a real/real-looking base (no bare gradient): yes
- mascot big/high with a specific pose+expression per scene, varied side (L/R/CL/R/L/C/C): yes (7 distinct poses)
- show-as-you-say timeline present per scene; "Attention in. Garbage out." lands as the payoff: yes
- every asset has type + description + filename + layout: yes
- callbacks are intentional and reuse the SAME filenames (engine, six-finger hand, too-perfect face): yes
- calmer than S5 (the flood recedes here); no spammy CTA; ends on WIT eyes open: yes
- public figures / brands: none; all callbacks are the generic AI-slop artifacts: yes
- no image-generation prompts here (handoff to visual-implement): yes
- timing ESTIMATED (no word-timings) - render must generate + re-pin: flagged
- in sync with master `04-visual-plan.md`: yes
