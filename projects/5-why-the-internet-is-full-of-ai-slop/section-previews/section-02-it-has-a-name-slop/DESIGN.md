# Section 2 It Has A Name: Slop - Design

Composition: `Section02Slop` (1920x1080, 36.715s). Audio: `section-02.mp3` (am_eric / 0.80).

## Look
- Channel board style: real bright base per scene + giant keyed-transparent WIT + handwritten labels.
- Names the phenomenon ("SLOP" stamp), then reframes: not pig-slop-only, not "evil robots", not a "master plan" - it's cheap content + pay-for-attention.
- Recurring motif: the grey-sludge flood (from S1) reused 2.1/2.5/2.8, graded per scene; sludge scenes darkened so the white WIT + yellow text pop (S1 contrast lesson applied up front).
- Idea-devices vary: SLOP stamp / pig + arrow / dictionary entry + WotY badge / crossed "evil robots" / deadpan caption / pinned "master plan" + strike / ATTENTION-vs-QUALITY chips / "STILL RISING".

## Timing
All reveals pinned to real word timings (`../../voiceover/section-02-it-has-a-name-slop/section-02-word-timings.json`).

## Assets
Shared library via `./assets` junction. Poses are transparent cutouts in `assets/poses/` (library pre-keyed).
Bases: grey-sludge (reuse), pig-trough, dictionary-open, toy-robot, corkboard-redstring, coins-pile.
Dictionary card, WotY badge, crossed labels, and attention/quality chips are CSS (real-UI), not stored assets.
