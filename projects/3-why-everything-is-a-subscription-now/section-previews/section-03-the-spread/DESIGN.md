# Section 3 The Spread - Design (2026-06-23, Section 1 template)

Video: `Why Everything Is a Subscription Now`
Composition: `Section03Spread` · 1920x1080 · duration `54.165s` · port `1003`

## Structure (5 big scenes, ~19 cues)

- A software (0–13.10): laptop desk (`base-desk`). `IT STARTED WITH SOFTWARE`; a CSS software window with a `BUY ONCE` tag that flips to `$9.99/mo`, then GREYS OUT + padlock + red ransom note `PAY ME, OR THE DOCUMENT GETS IT`; WIT hidden-fee-panic CENTER-RIGHT.
- B streaming (13.10–24.70): living-room TV (`base-tv-room`). A colorful CSS streaming-tile wall fills, then VANISHES; labels `you don't OWN a song` / `you RENT a giant library`; `POV: YOU OWN NOTHING` card; WIT shocked LEFT.
- C five subs (24.70–35.20): euro cash (`base-cash`). `NEVER JUST ONE. IT'S FIVE.`; five subscription tiles pop one-per-item; `each one feels small…`. No WIT (breathes).
- D dungeon (35.20–41.40): jail corridor (`base-jail`). `five subs > OLD CABLE BILL`; `escaped 1 dungeon →` / `built 5 smaller ones`; WIT trapped-by-app-screen CENTER (the pose's screen-frame doubles as a cell).
- E car (41.40–54.165): car interior (`base-car`). `IT DIDN'T STOP AT SCREENS`; a CSS `HEATED SEAT` button + `seats you already bought`; padlock + `$9.99/mo`; an EXPIRED system banner `your free trial of a WARM BOTTOM has expired` (running-gag callback); WIT deadpan-side-eye RIGHT.

## Vivid object bases (one per scene)

laptop desk → living-room TV → euro cash → jail corridor → car interior. All CC0 via Openverse; the car is a
mockup target (small Blaupunkt logo covered by the seat panel + WIT).

## Voice / timing

- Word timings GENERATED this run (transformers.js whisper-tiny.en); pinned to word starts; duration capped at 54.165 (tail overshoot ignored). Key pins: monthly flip @4.8, grey @8.0, padlock @10.3, ransom @11.3, vanish @21.8, POV @23.7, 5 tiles @27.6–31.9, cable-compare @35.5, built @39.7, heated seat @45.4, fee/lock @49.0, EXPIRED @50.9.

## Motion / WIT

- hard-show labels; impact for the price flip, padlock slams, the wall vanish, the cost comparison, and the EXPIRED banner.
- 4 giant WIT beats, varied side/pose (hidden-fee-panic CR → shocked L → trapped C → deadpan-side-eye R); BS3 breathes. AVOID `typing-on-laptop` / `money-panic` (baked black bg).
