# Section 6 Reference Board (2026-06-23)

Visual reference pass for Section 6 (the cancel-maze / negative-option-billing section). Real bases sourced
via Openverse / Wikimedia and VIEWED before selection.

## Reference Pass Status

- Status: `complete (real images via Openverse / Wikimedia; viewed before selection)`
- Real bases saved: 3 distinct (stopwatch, maze, contract); stopwatch + maze each duplicated to a 2nd
  filename for a deliberate darker thematic callback in the tail beats (sourcing of distinct tail objects failed - see below)
- Generated images: none (no generator connected)

## Big-scene bases

| Scene | Base | What it shows | Why it fits | Source / license |
|---|---|---|---|---|
| BS1 easy-in/hard-out | base-stopwatch.jpg | a silver sports stopwatch | "signing up takes 10 seconds…" - the time asymmetry | CC0, rawpixel (Openverse) |
| BS2 the maze | base-maze.jpg | a wooden labyrinth/marble maze game | "a vision quest / final boss / wandering the menus" - the cancel ordeal | CC0, rawpixel (Openverse) |
| BS3 negative option | base-contract.jpg | a CONTRACT with fine print + pen | "negative option billing" - the fine-print trick | CC BY, Wikimedia "Legal Contract & Signature" |
| BS4 burden / −1000 aura | base-maze-2.jpg | the maze again, dark (callback) | "all pushed onto you, −1000 aura, still lost" | CC0 (copy of base-maze) |
| BS5 give up / no salary | base-stopwatch-2.jpg | the stopwatch again, dark (callback) | "a part-time job [time] with no salary" | CC0 (copy of base-stopwatch) |

## Sourcing note (the two callbacks)

Clean CC photos for the abstract tail beats - "the unfair burden / −1000 aura points" and "a part-time job
with no salary" - were not findable this session (Openverse returned empty/irrelevant for clock, punch-card,
white-flag, etc.). Rather than ship a sterile or off-tone base, BS4 and BS5 REUSE the section's two
controlling metaphors (the maze = the ongoing ordeal; the stopwatch = the time cost) as deliberate,
darker-graded callbacks with different CSS devices + WIT, so they read as new beats. The owner can drop
replacement photos into `assets/visual-references/section-06-easy-in-no-way-out/` for these two if preferred.

## Inspected and rejected

- A phone shot showing a person's head/beard (no-face channel). reject.
- "Free Labyrinth Maze" search hit a Holocaust-memorial photo - off-tone, not a maze. reject.
- A museum punch-clock display (cluttered with exhibit text panels). reject.
- Alarm-clock / labyrinth / stopwatch PNG "sticker" illustrations (not real photos). reject.
- "White flag" search returned historical war photos. reject.

## CSS idea-devices

green/red SIGN-UP vs CANCEL labels; a 7-step menu breadcrumb trail (the real cancel steps) winding through
the maze; a red `NEGATIVE OPTION BILLING` stamp; a game-HUD `−1000 AURA`; a `part-time job / salary $0` payoff.

## WIT poses used (shared manifest)

running-away (fleeing the menu maze), suspicious (the trick), confused (lost / −1000 aura), tiny-defeated
(gave up). All verified transparent. AVOIDED: `typing-on-laptop` + `money-panic` (baked black bg).
