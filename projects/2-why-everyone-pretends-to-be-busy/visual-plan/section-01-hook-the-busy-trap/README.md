# Section 1 Visual Plan - Hook: The Busy Trap (v2 remake)

Files:
- `section-01-hook-the-busy-trap-visual-plan.md` - scene/cue plan (4 scenes, 10 cues)
- `reference-board.md` - reference pass (4 real bases + real-UI calendar/inbox/phone)

Render lives at:
`section-previews/section-01-hook-the-busy-trap/` (port 1001)

Key decisions (remade from scratch to the new standard):
- Real photo base behind every scene; real-UI (packed calendar / unread inbox / phone panic) for the "busy signals" beat.
- WIT big (≈1/3–1/2 frame) and high (head+torso inside frame); labels arranged around it.
- 4 distinct WIT poses: typing → phone-panic → deadpan → suspicious. D returns to the warm desk + CSS cage bars (callback to A).
- Cues pinned to existing `section-01-word-timings.json` (0.84 audio, 21.12s). Audio is 0.84 vs the rest at 0.86 - flagged; re-pin if regenerated.
