# Section 4 Why Companies Love It — Design (2026-06-23, Section 1 template)

Video: `Why Everything Is a Subscription Now`
Composition: `Section04Why` · 1920x1080 · duration `51.093s` · port `1004`

## Structure (6 big scenes, ~15 cues)

- A follow the money (0–5.0): $100 cash. `FOLLOW THE MONEY` gold kinetic word. No WIT.
- B coffee one-sale (5.0–12.0): espresso machine. `SO YOU SELL A COFFEE MACHINE`; one gold coin + `ONE sale. thanks!`; `then you wait… maybe YEARS`; WIT sleeping-burned-out RIGHT.
- C recurring machine (12.0–22.0): same machine (warm). `NOW: EVERY MONTH`; a rising COIN GEYSER; `automatically` → `FOREVER.`; cream aside `which would you rather own? …me too`; WIT empty-wallet LEFT.
- D worth a lot (22.0–30.6): cash. `buys once = a little` + small coin vs `pays monthly = A LOT` + coin stack; coin RAIN + cream aside `like rain. or relatives`. No WIT.
- E recurring word (30.6–39.1): calendar. `the magic word:` → giant `RECURRING`; an `AUTO-PAY · THE SAME CHARGE, EVERY MONTH` statement card (identical −$9.99 rows Jan/Feb/Mar/Apr pop one-per-beat = "recurring"); `plan around your FORGETFULNESS`; WIT confused LEFT (text + card on the right). [Review fix: the card replaced 4 red rings that circled nothing.]
- F beautiful trap (39.1–51.093): mousetrap + cheese. `THE GOAL QUIETLY CHANGED`; `old: good enough to BUY` / `now: sticky enough you NEVER LEAVE`; payoff `more a BEAUTIFUL TRAP` (labels hidden at payoff); WIT suspicious RIGHT.

## Vivid object bases (one per scene)

$100 cash → espresso machine → same machine (before/after) → cash → calendar → mousetrap. All CC0 via
Openverse. Reused bases (cash BS1↔BS4 non-consecutive; coffee BS2→BS3 before/after) use 2nd filenames to keep lint clean.

## Voice / timing

- Word timings GENERATED this run (whisper-tiny.en); pinned to CLEAN word starts (the 24–26s backward-jump glitch avoided); duration capped at 51.093. Key pins: FOLLOW THE MONEY @2.9, one coin @7.0, wait-years @9.5, geyser @13.8, FOREVER @16.5, worth-a-lot @26.8, rain @29.3, RECURRING @33.3, forgetfulness @38.1, never-leave @44.8, BEAUTIFUL TRAP @50.0.

## Motion / WIT

- hard-show labels; impact for the kinetic words, the geyser, FOREVER, the comparison, and the payoff.
- 4 giant WIT beats, varied side/pose (sleeping-burned-out R → empty-wallet L → confused L → suspicious R); BS1 + BS4 breathe. AVOID `typing-on-laptop` / `money-panic` (baked black bg).
