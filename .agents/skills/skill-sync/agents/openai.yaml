interface:
  display_name: "Skill Sync"
  short_description: "Sync skills and root docs between Codex and Claude"
  default_prompt: "Use $skill-sync to reconcile the .agents and .claude skill wrappers and sync AGENTS.md with CLAUDE.md."
