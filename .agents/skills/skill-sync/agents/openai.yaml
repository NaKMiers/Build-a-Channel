interface:
  display_name: "Skill Sync"
  short_description: "Regenerate the Claude wrappers from .agents/skills"
  default_prompt: "Use $skill-sync to reconcile the Claude wrappers with the canonical skills."
