interface:
  display_name: "Script Draft"
  short_description: "Draft sectioned video scripts"
  default_prompt: "Use $script-draft to write the sectioned script draft for the selected Why It Works video project."
