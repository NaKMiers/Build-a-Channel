# Check Memory

- Every generated-image defect that could have been caught mechanically should become a
  permanent validation rule.
- The first accepted HumanPrice project will become the regression fixture.
- Until that fixture exists, report structural validation separately from end-to-end
  project validation.
