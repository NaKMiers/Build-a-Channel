# WIT Character Assets

Reusable reference assets for `WIT`, the audience-surrogate character for `Why It Works`.

## Current Status

Status: `draft replacement generated - awaiting user review`

There is currently a draft reusable WIT pose set:

```text
.agents/_shared/assets/wit/poses/thumbnail-wit-24/
```

Contact sheet:

```text
.agents/_shared/assets/wit/poses/thumbnail-wit-24/thumbnail-wit-24-contact-sheet.png
```

The previous current pose folder was removed on `2026-06-07`:

```text
.agents/_shared/assets/wit/poses/original-wit-24/
```

Reason:
the user decided the WIT style in the five restored `Why Cheap Products Keep Getting Worse` thumbnail drafts is stronger than the previous channel WIT.

## Current Direction

The draft WIT pose set was generated from the restored cheap-products thumbnails.

Target character direction:

- simple white round-headed cartoon figure
- thick imperfect black outline
- oversized black glasses
- expressive eyebrows
- small dot eyes
- simple white body
- strong suspicious, betrayed, trapped, panicked, confused, and defeated reactions
- readable as a thumbnail character before becoming a full production pose set

## Active References

Use the restored thumbnail drafts in:

```text
projects/why-cheap-products-keep-getting-worse/assets/thumbnails/
```

Do not use removed `original-wit-24` assets as current channel WIT.

## Next Step

Wait for user review and requested adjustments.

Do not mark `thumbnail-wit-24` as final until the user approves it.
