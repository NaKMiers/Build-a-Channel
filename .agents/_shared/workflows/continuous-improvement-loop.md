# Continuous Improvement Loop

The core of this workspace is continuous improvement.

Every video should improve:

- scripts
- hooks
- titles
- thumbnails
- visual systems
- voiceover
- production speed
- audience understanding

## After Each Video

Update the video's:

```text
projects/<slug>/06-review.md
```

Capture:

- what worked
- what felt weak
- viewer signals
- production bottlenecks
- improvements for next time

Then update:

- `.agents/_shared/channel/learning-log.md` for reusable lessons
- `.agents/_shared/workflows/` if the workflow changed
- `.agents/_shared/templates/` if the template should improve
- `.agents/_shared/tools/` if a tool should become reusable

## Rule

Do not let lessons stay only in chat.

If a lesson can make the next video better, write it down.
