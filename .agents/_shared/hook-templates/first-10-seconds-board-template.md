# First 10 Seconds Board Template

Use this only for future per-video hook planning after the user explicitly asks to work on a specific video.

This template is channel-wide. Do not fill it for an existing video unless the user explicitly asks to apply the hook system to that video.

## Video Context

Plain topic:

```text
_____
```

Chosen title:

```text
_____
```

Thumbnail promise:

```text
_____
```

Dominant thumbnail object:

```text
_____
```

Main contradiction:

```text
_____
```

WIT emotion:

```text
suspicious / betrayed / trapped / panicked / confused / defeated / deadpan
```

Hook type:

```text
suspicious object / false promise / tiny problem, huge system / emotional receipt
```

## First Spoken Line

Draft line:

```text
_____
```

Line check:

```text
[ ] simple
[ ] specific
[ ] sounds like a person
[ ] contains a contradiction
[ ] avoids slow background explanation
```

## Board 1: Topic Object

Time:

```text
0:00-0:03
```

Job:

```text
Make the topic readable with no audio.
```

Visual:

```text
_____
```

On-screen text:

```text
_____
```

Motion:

```text
hard cut / tiny object motion / none
```

Mobile check:

```text
[ ] one dominant object
[ ] text is readable
[ ] topic is clear by second 3
```

## Board 2: Contradiction

Time:

```text
0:03-0:06
```

Job:

```text
Make the normal object suspicious.
```

Visual:

```text
_____
```

On-screen text:

```text
_____
```

Suspicious detail:

```text
hidden cost / extra rule / trap / downgrade / timer / receipt / warning / other
```

Mobile check:

```text
[ ] contradiction is visible with no audio
[ ] viewer has a question by second 5
[ ] no complex dashboard or tiny UI
```

## Board 3: WIT Reaction

Time:

```text
0:06-0:08
```

Job:

```text
Show how the system feels to a normal person.
```

WIT pose:

```text
_____
```

WIT action:

```text
_____
```

On-screen text:

```text
_____
```

Mobile check:

```text
[ ] WIT emotion is clear
[ ] WIT reacts to the contradiction
[ ] WIT is not a neutral presenter
```

## Board 4: Video Promise

Time:

```text
0:08-0:10
```

Job:

```text
Turn the object into the bigger question.
```

Visual:

```text
_____
```

On-screen text:

```text
_____
```

Narration turn:

```text
_____
```

Promise check:

```text
[ ] thumbnail promise is paid off by second 10
[ ] key phrase matches the video topic
[ ] the viewer knows why to keep watching
```

## Sound And Pace

Voice direction:

```text
clear / dry / slightly suspicious / learner-friendly
```

Sound effects:

```text
none / one tiny hit / receipt print / marker squeak / phone buzz / other
```

Pacing note:

```text
_____
```

## Hook Summary

One-sentence hook promise:

```text
By second 10, the viewer understands that _____ is suspicious because _____.
```

What question remains?

```text
_____
```

What should the viewer expect next?

```text
_____
```
